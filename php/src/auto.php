<?php

declare(strict_types=1);

// Loaded automatically via composer.json's `autoload.files` — the one thing
// a PHP consumer does NOT have to write themselves. Runs for both FPM and
// CLI, since both go through `require vendor/autoload.php`.

\Unilog\Installer::install();
