{
    "name": "sanprojects/unilog",
    "description": "Zero-config OTel-shaped structured logging for PHP (CLI, FPM, Workerman, Swoole).",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": ">=8.1"
    },
    "suggest": {
        "psr/log": "Required only if you use Unilog\\Psr3\\Logger",
        "monolog/monolog": "Required only if you use Unilog\\Monolog\\UnilogHandler"
    },
    "require-dev": {
        "psr/log": "^2.0 || ^3.0",
        "monolog/monolog": "^3.0"
    },
    "autoload": {
        "psr-4": {
            "Unilog\\": "php/src/"
        },
        "files": [
            "php/src/auto.php"
        ]
    }
}
